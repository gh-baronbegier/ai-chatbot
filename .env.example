# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on Vercel

# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/postgres
POSTGRES_URL=****


# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****

# Claude MAX OAuth (flat-rate Claude subscription)
# Run `npx anthropic-max-router` once locally, then copy refresh_token from .oauth-tokens.json
CLAUDE_MAX_REFRESH_TOKEN=

# OpenAI Codex OAuth (ChatGPT Plus/Pro subscription)
# Set refresh token from your Codex/OpenAI OAuth login flow
OPENAI_CODEX_REFRESH_TOKEN=
# Optional override for Codex OAuth client ID (default set in code)
OPENAI_CODEX_CLIENT_ID=

# Upstash Redis (for Baron's live location tracking + Claude MAX token caching)
UPSTASH_REDIS_URL=
UPSTASH_REDIS_TOKEN=

# Mapbox (for reverse geocoding)
MAPBOX_TOKEN=

# Neon MCP (for natural language database management)
NEON_API_KEY=

# GitHub MCP (for repo management, issues, PRs, actions)
# Create a PAT at https://github.com/settings/tokens
GITHUB_PERSONAL_ACCESS_TOKEN=

# Vercel MCP (for deployment logs, project management)
# Create a token at https://vercel.com/account/tokens
VERCEL_MCP_TOKEN=

# Notion MCP (for workspace search, page creation, task management)
# Create an integration at https://www.notion.so/my-integrations
NOTION_MCP_TOKEN=

# Terraform MCP (for provider docs, modules, registry access)
# Run: npx @hashicorp/terraform-mcp-server --transport http --port 8080
TERRAFORM_MCP_URL=

# AWS MCP (for AWS docs, CDK, Bedrock, cost analysis)
# See: https://github.com/awslabs/mcp
AWS_MCP_URL=

# Exa MCP (for AI-optimized web search, code search, company research)
# Get your key at https://exa.ai
EXA_API_KEY=

# Alpha Vantage MCP (for stock market data, forex, crypto, technical indicators)
# Get your free key at https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=

# Google Maps MCP (for place search, weather, route computation)
# Enable Maps Grounding Lite API in Google Cloud Console
GOOGLE_MAPS_API_KEY=

# Stripe MCP (for payments, customers, invoices, subscriptions)
# Get your key at https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=

# E2B (for secure Python code execution in sandboxes)
# Get your key at https://e2b.dev
E2B_API_KEY=

# Linear MCP (for issues, projects, comments, and team management)
# Create a Personal API key at Linear Settings > Account > Security & Access
LINEAR_API_KEY=

# Groq (fallback model — direct API)
# Get your key at https://console.groq.com
GROQ_API_KEY=

# OpenAI-compatible API layer (/api/v1/chat/completions)
# Optional: if not set, all requests are accepted without auth
OPENAI_COMPAT_API_KEY=
